declare const keyframes: any;
declare const css: any;

declare const theColor: any;

const key = keyframes`
  to {
    transform: rotate(360deg);
  }
`;

const color = css`
  color: ${theColor};
`;

export {};
